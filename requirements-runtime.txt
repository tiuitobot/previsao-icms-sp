# Pipeline Engine runtime dependencies
jsonschema>=4.20.0
rich>=13.0.0

# Condominium audit pipeline — Excel ingestion
openpyxl>=3.1.0

# Optional: LLM provider SDKs (install only what you need)
# openai>=1.30.0
# anthropic>=0.25.0
